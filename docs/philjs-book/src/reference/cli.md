# CLI

PhilJS uses the `create-philjs` starter for new projects.

```bash
pnpm create philjs my-app
```

Common flags:

- `--template` to choose a starter
- `--router` to include the router
- `--ssr` to include SSR defaults
