import type { Migration } from '../types';

export default {
  name: 'batch1_migration1',

  async up(context) {
    // Write migration here
  },

  async down(context) {
    // Write rollback here
  },
} as Migration;
