import type { Migration } from '../types';

export default {
  name: 'migration_1',

  async up(context) {
    // Write migration here
  },

  async down(context) {
    // Write rollback here
  },
} as Migration;
