
// Stub for Clerk Elements styling wrapper
export function SignIn(props: { appearance: any }) {
    // Wraps @clerk/clerk-react <SignIn /> with local class names
    return \`<div class="phil-clerk-signin">Clerk Sign In (Styled)</div>\`;
}
