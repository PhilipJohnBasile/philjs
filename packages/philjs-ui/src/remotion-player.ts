
// Stub for Remotion Player
export function RemotionPlayer(props: { composition: any, fps: number }) {
    // Would wrap @remotion/player
    return \`<div class="phil-remotion-player">Playing sequence at \${props.fps}fps</div>\`;
}
