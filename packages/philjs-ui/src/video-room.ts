
// Stub for LiveKit Video Room
export function VideoRoom(props: { token: string }) {
    // Should initiate websocket connection to LiveKit server
    return \`<div class="phil-live-kit-room" data-token="\${props.token}">Connecting to Video...</div>\`;
}
