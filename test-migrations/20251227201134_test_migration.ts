import type { Migration } from '../types';

export default {
  name: 'test_migration',

  async up(context) {
    // Write migration here
  },

  async down(context) {
    // Write rollback here
  },
} as Migration;
